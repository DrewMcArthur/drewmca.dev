---
import { sections } from "../../../data/tridentConfig.json";
const section = Astro.props.section as number;
const linkUrl = `/projects/trident/section/${section}`;
---

<a href={linkUrl}>{section}. {sections[section]?.title}</a>
